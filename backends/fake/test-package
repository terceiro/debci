#!/usr/bin/ruby

puts "Not really running anything .."
puts "This will fail, pass or tmpfail randomly."

r = rand()

if (r <= 0.7)
  puts "Passed :-)"
  exit 0
elsif (r <= 0.85)
  puts "Failed :-("
  exit 4
else
  puts "Some error ocurred"
  exit 16
end

